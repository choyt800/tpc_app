<table class='total-amount'>
  <tr>
    <td>Sub-total:</td>
    <td><span class='subtotal'>$0.00</span></td>
  </tr>

  <tr>
    <td>Coupon:</td>
    <td><%= f.select :coupon, @coupons.collect {|c| c.id}, {include_blank: true}, {class: 'coupon'} %></td>
  </tr>

  <tr>
    <td>First Bill Date:</td>
    <td><%= f.text_field :trial_period_days, value: Date.current %></td>
  </tr>

  <tr>
    <td colspan=2>
      <%= button_tag 'Preview', id: 'preview-invoice', disabled: true %>
    </td>
  </tr>

  <tr><td>&nbsp;</td></tr>

  <tr>
    <td>Discount:</td>
    <td><span class='discount'>$0.00</span></td>
  </tr>

  <tr>
    <td>Total:</td>
    <td><span class='total'>$0.00</span></td>
  </tr>

  <tr>
    <td colspan=2>
      <% if @member.stripe_id %>
        <%= f.submit name: 'payment_type', value: 'Create - Pay via Stripe', class: 'create-subscription', id: 'create-subscription-stripe', disabled: true, remote: true, data: { confirm: "WARNING: If this is a Stripe plan, this will charge the Member's Credit Card IMMEDIATELY." } %>
      <% end %>
      <%= f.submit name: 'payment_type', value: 'Create - Pay via Check', class: 'create-subscription', id: 'create-subscription-check', disabled: true, remote: true %>
    </td>
  </tr>
</table>
